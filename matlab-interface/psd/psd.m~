function [PSD]=psd(U1,U2,dns)

% Remove mean (is this really wanted?)
U1(0,0,:)=0;
U2(0,0,:)=0;

% Check size and declare stuff
if size(U1)~=size(U2)
    error('Size of U1 and U2 must be same')
end
PSD=zeros(size(U1));

% 
